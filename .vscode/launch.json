{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Preview Site (Python HTTP Server)",
            "type": "python",
            "request": "launch",
            "program": "-m",
            "args": ["http.server", "8000"],
            "console": "integratedTerminal",
            "justMyCode": false,
            "cwd": "${workspaceFolder}"
        }
    ]
}
